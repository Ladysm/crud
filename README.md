# crud
PROYECTOGO/
├── api/
│   ├── router/
│   │   └── router.go     → Define las rutas del API
│   └── server.go         → Configura y arranca el servidor HTTP
├── cmd/
│   └── main.go           → Punto de entrada del programa (función main)
├── go.mod / go.sum       → Archivos de dependencias
└── README.md             → Descripción del proyecto
